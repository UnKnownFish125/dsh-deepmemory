// Minimal preset-plane plugin entry point.
// Replace this file or keep it as the starting point for a child plugin.

export function apply(ctx) {
  ctx.logger?.info?.('[blank-template] plugin loaded');
}
